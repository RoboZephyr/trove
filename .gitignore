# Dependencies
node_modules/
dist/
build/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local secrets / test data — NEVER commit real credentials
# (matches both ~/.trove/<svc>/ and any project-level .trove/<svc>/ files)
**/credentials.json
**/credentials.enc
*.env
*.env.local
.dev.vars
.dev.vars.*

# Lark CLI config (if it ends up adjacent to a project)
.lark-cli/

# Test output
coverage/
*.log

# Tauri / Rust (if GUI later)
target/

# OSS hygiene
.envrc
